<!doctype html><html><head><meta charset="utf-8"><title>Edit {{ $version->document->title }}</title></head><body><h1>{{ $version->document->title }} — {{ $version->version_label }}</h1><p>Status: {{ $version->status->value }} · checksum {{ $version->content_checksum }}</p><form method="post">@csrf @method('PUT')<label>Change summary <input name="change_summary" required value="{{ old('change_summary',$version->change_summary) }}"></label><br><textarea name="markdown_source" rows="35" cols="120" required>{{ old('markdown_source',$version->markdown_source) }}</textarea><br><button>Save Draft</button></form><h2>Rendered preview</h2><article>{!! $version->sanitized_html !!}</article><h2>Plain text</h2><pre>{{ $version->plain_text }}</pre></body></html>
