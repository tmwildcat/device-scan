<!doctype html><html><head><meta charset="utf-8"><title>Legal Governance — {{ $section }}</title></head><body><h1>{{ ucwords(str_replace('-',' ',$section)) }}</h1><a href="{{ route('legal-governance.dashboard') }}">Dashboard</a><pre>{{ json_encode(method_exists($payload,'items')?$payload->items():$payload,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) }}</pre>@if(method_exists($payload,'links')){{ $payload->links() }}@endif</body></html>
